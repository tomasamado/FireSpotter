import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    principalItem: {
        fontSize: 20,
        padding: 5,
        width:300
    },
    textStyle: {
        padding: 5,
        marginLeft: 2,
        marginTop: 5,
        fontSize: 15,
    },
    container: {
        marginBottom: 40,
    }
});

export default styles