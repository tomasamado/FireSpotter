import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    principalItem: {
        fontSize: 18,
        padding: 5,
        width:250,
        marginRight: 10,

    },
    textStyle: {
        padding: 5,
        marginLeft: 2,
        marginTop: 5,
        fontSize: 15,
    },
    container: {
        marginBottom: 40,
    }
});

export default styles